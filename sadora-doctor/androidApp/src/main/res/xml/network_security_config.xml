<?xml version="1.0" encoding="utf-8"?>
<!--
  The client app's config. Cleartext is off everywhere except the addresses that only
  exist on a developer's machine: 10.0.2.2 is how the emulator reaches its host, and
  localhost is a phone running against the backend through `adb reverse`. Neither
  resolves to anything in production, so the exception cannot follow the app to a doctor.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
