<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window paints the app's own ground while Compose starts, so there is no
         white flash before the splash. -->
    <style name="Theme.SadoraDoctor" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
