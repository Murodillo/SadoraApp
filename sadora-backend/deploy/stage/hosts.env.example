# Copy to deploy/stage/hosts.env (gitignored) and fill in. The repository is public, so
# the addresses of the staging box and the host in front of it stay out of it.
SADORA_JUMP=root@jump.example
SADORA_HOST=root@10.0.0.2
